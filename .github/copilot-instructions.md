# Copilot Instructions

## Project Guidelines
- User uses `data-type` attributes (not `data-id`) as the convention for targeting form fields/sections in HTML/CSS/JS.
- Communicate in Swedish.
- Reuse existing error handling patterns in the codebase; avoid duplicating error-message handling logic in controllers.